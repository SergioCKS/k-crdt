{
  "name": "rust-wasm",
  "version": "0.0.0",
  "scripts": {
    "build": "tsc && node build.js"
  },
  "devDependencies": {
    "typescript": "^4.3.2",
    "@cloudflare/workers-types": "^3.0.0"
  }
}
